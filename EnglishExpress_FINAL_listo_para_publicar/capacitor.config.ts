import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // Identificador único de tu app. NO lo cambies después de publicar.
  // Formato recomendado: com.tunombre.englishexpress
  appId: 'com.englishexpress.app',
  appName: 'EnglishExpress',
  webDir: 'dist',
};

export default config;
